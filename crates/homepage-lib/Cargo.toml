[package]
name = "homepage-lib"
version = "0.1.0"
edition = "2024"

[lib]
name = "homepage"
crate-type = ["cdylib"]
path = "lib.rs"

[dependencies]
homepage = {path = "../homepage", features = ["live"]}
